// app.js — Configuration Express
// TODO: importer express, helmet, cors, morgan, cookie-parser
// TODO: importer rateLimitConfig depuis config/security.js
// TODO: importer toutes les routes (auth, admin, client)
// TODO: importer le middleware global d'erreurs
// TODO: appliquer helmet(), cors(), express.json(), cookie-parser
// TODO: monter /api/auth  /api/admin  /api/client
// TODO: middleware 404 + error handler global
// module.exports = app
