// models/notification.model.js
// Notifications in-app pour les clients
// Colonnes : id, userId (FK), titre, message, type (enum: colis|expedition|paiement|alerte),
//            isRead, lienCible, createdAt
